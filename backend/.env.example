# Database Configuration
# SQLite (default - no setup required)
DATABASE_URL=sqlite:///./invoice_recovery.db

# PostgreSQL (uncomment and configure if needed)
# DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/invoice_recovery

# Application Settings
APP_NAME=Invoice Recovery Tracker
DEBUG=True
